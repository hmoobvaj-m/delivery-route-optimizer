name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  repo-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Check repository structure
        run: |
          test -f README.md
          test -f .gitignore
          test -f .env.example

      - name: Prevent committed secrets by filename
        run: |
          ! find . -name ".env" -o -name "*.pem" -o -name "*.key"